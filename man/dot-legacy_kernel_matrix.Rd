% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel.R
\name{.legacy_kernel_matrix}
\alias{.legacy_kernel_matrix}
\title{Pure-R nested-loop kernel matrix (fallback path)}
\usage{
.legacy_kernel_matrix(K, X1, X2)
}
\arguments{
\item{K}{A kernel function from \code{\link[=make_kernel]{make_kernel()}}.}

\item{X1}{Numeric matrix with one observation per row (n1 × p).}

\item{X2}{Numeric matrix with one observation per row (n2 × p).
Defaults to \code{X1}, giving the square training kernel matrix Ω.}
}
\value{
Numeric matrix of size n1 × n2.
}
\description{
Original \code{\link[=kernel_matrix]{kernel_matrix()}} body, retained as a fallback for kernel
closures that do not carry a \code{kernel_info} attribute (i.e., not built
via \code{\link[=make_kernel]{make_kernel()}}). Also used by tests to verify Rcpp-vs-R parity.
}
\keyword{internal}
