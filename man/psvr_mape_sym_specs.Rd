% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip.R
\name{psvr_mape_sym_specs}
\alias{psvr_mape_sym_specs}
\alias{psvr_mape_sym_rbf}
\alias{psvr_mape_sym_poly}
\alias{psvr_mape_sym_linear}
\title{Parsnip model specs: symmetric epsilon-SVR with MAPE loss (Model 2)}
\usage{
psvr_mape_sym_rbf(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  svm_margin = NULL,
  rbf_sigma = NULL
)

psvr_mape_sym_poly(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  svm_margin = NULL,
  degree = NULL,
  scale_factor = NULL
)

psvr_mape_sym_linear(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  svm_margin = NULL
)
}
\arguments{
\item{mode}{Only \code{"regression"} is supported.}

\item{engine}{Only \code{"psvr"} is available.}

\item{cost}{Regularization parameter \code{C > 0}.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.}

\item{svm_margin}{Epsilon tube half-width \verb{ε ≥ 0} (percentage units).
Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.}

\item{rbf_sigma}{RBF bandwidth σ > 0.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
(RBF specs only.)}

\item{degree}{Polynomial degree ≥ 1.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
(Polynomial specs only.)}

\item{scale_factor}{Polynomial constant term (coef₀).  Use \code{\link[hardhat:tune]{hardhat::tune()}} to
optimize.  (Polynomial specs only.)}
}
\value{
A parsnip \code{model_spec} object of the corresponding class.
}
\description{
Create parsnip model specifications for \code{\link[=mape_sym_svr]{mape_sym_svr()}} with a fixed
kernel type.  The symmetry parameter \code{a} is an engine argument passed via
\code{set_engine("psvr", a = 1L)}.
}
\examples{
\dontrun{
library(parsnip)
spec <- psvr_mape_sym_rbf(cost = 10, svm_margin = 1, rbf_sigma = 1) |>
  set_engine("psvr", a = 1L)

spec_poly <- psvr_mape_sym_poly(cost = 10, svm_margin = 1, degree = 2,
                                scale_factor = 1) |>
  set_engine("psvr", a = 1L)

spec_lin <- psvr_mape_sym_linear(cost = 10, svm_margin = 1) |>
  set_engine("psvr", a = 1L)
}

}
