% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{psvr_option_add_cost_ls}
\alias{psvr_option_add_cost_ls}
\title{Apply data-driven LS-SVR cost range to all m3/m4 workflows in a workflow set}
\usage{
psvr_option_add_cost_ls(wf_set, y, width_log2 = 4)
}
\arguments{
\item{wf_set}{A \code{workflow_set} object.}

\item{y}{Numeric vector of strictly positive training targets.}

\item{width_log2}{Passed to \code{\link[=cost_psvr_ls_data]{cost_psvr_ls_data()}}. Default \code{4}.}
}
\value{
The updated \code{workflow_set}.
}
\description{
A convenience wrapper that calls \code{\link[workflowsets:option_add]{workflowsets::option_add()}} for
every LS-SVR psvr workflow in \code{wf_set} (those whose \code{wflow_id}
matches \code{"m3"} or \code{"m4"}), replacing the \code{cost} dials parameter
with one built from \code{y} via \code{\link[=cost_psvr_ls_data]{cost_psvr_ls_data()}}.
}
\details{
Workflows for \code{m1}/\code{m2} (\code{ε}-SVR) are intentionally skipped — for
those, \code{cost} maps to \code{C} and the static \code{\link[=cost_psvr]{cost_psvr()}} range is
usually adequate.

Note: \code{\link[workflowsets:option_add]{workflowsets::option_add()}} replaces the whole \code{param_info}
option for each matched workflow.  If you also need a data-driven
\code{rbf_sigma} (via \code{\link[=psvr_option_add]{psvr_option_add()}}), build the full \code{param_info}
manually with \code{\link[tune:reexports]{tune::extract_parameter_set_dials()}} and call
\code{\link[workflowsets:option_add]{workflowsets::option_add()}} in one shot, or call this helper
first and then \code{\link[=psvr_option_add]{psvr_option_add()}} (which only touches
\code{rbf_sigma}) — the latter currently overwrites the former, so
prefer the manual one-shot approach when both are needed.
}
\examples{
\dontrun{
# After building wf_set with at least one m3 or m4 workflow:
wf_set <- psvr_option_add_cost_ls(wf_set, y = train_df$y)
}

}
\seealso{
\code{\link[=cost_psvr_ls_data]{cost_psvr_ls_data()}}, \code{\link[=psvr_option_add]{psvr_option_add()}}
}
