% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel.R
\name{sym_kernel_block}
\alias{sym_kernel_block}
\title{Compute a symmetric kernel block for prediction}
\usage{
sym_kernel_block(K, X, Xnew, a)
}
\arguments{
\item{K}{A kernel function from \code{\link[=make_kernel]{make_kernel()}}.}

\item{X}{Numeric training matrix (N × p).}

\item{Xnew}{Numeric matrix of new points (M × p).}

\item{a}{Symmetry parameter: \code{1} (even) or \code{-1} (odd).}
}
\value{
Numeric N × M matrix.
}
\description{
Block form of \code{\link[=sym_kernel_vector]{sym_kernel_vector()}}: entry \verb{[k, i]} equals
\verb{½ * Ks(X[k, ], Xnew[i, ])} where \code{Ks(xi, xj) = K(xi, xj) + a * K(xi, -xj)}.
Used by the \code{predict()} methods of the symmetric models (Models 2 and 4).
}
\details{
Built from two \code{\link[=kernel_matrix]{kernel_matrix()}} calls rather than a nested R loop, so
built-in kernels reach the Rcpp implementations; user-defined closures
still fall through to \code{.legacy_kernel_matrix()}. Element-wise this is the
same \code{0.5 * (K + a * K_neg)} arithmetic in the same order as the per-row
loop it replaces, so predictions are bit-identical.
}
\keyword{internal}
