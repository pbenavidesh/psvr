% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{sigma_heuristic}
\alias{sigma_heuristic}
\title{Median-distance heuristic for RBF kernel bandwidth}
\usage{
sigma_heuristic(X, sample_size = 500L, seed = NULL)
}
\arguments{
\item{X}{A numeric matrix or data frame of predictors (already
preprocessed — centred, scaled, etc.).}

\item{sample_size}{Integer. If \code{nrow(X) > sample_size}, a random
subsample is used to avoid O(n²) memory cost on large datasets.
Default \code{500L}.}

\item{seed}{Integer seed for the subsample. Default \code{NULL}.}
}
\value{
A scalar numeric: the median pairwise Euclidean distance.
}
\description{
Returns the median pairwise Euclidean distance between rows of \code{X},
which is a standard data-driven starting point for the RBF kernel
bandwidth (Schölkopf & Smola, 2002). Use the result to define a
sensible \code{rbf_sigma} search range centred on this value via
\code{dials::rbf_sigma(range = c(log10(sigma / 10), log10(sigma * 10)))}.
}
\examples{
X <- matrix(rnorm(200), ncol = 4)
sigma_heuristic(X)

}
