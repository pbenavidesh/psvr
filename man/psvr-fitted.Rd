% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitted-residuals.R
\name{psvr-fitted}
\alias{psvr-fitted}
\alias{fitted.psvr_mape}
\alias{fitted.psvr_mape_sym}
\alias{fitted.psvr_rmspe}
\alias{fitted.psvr_rmspe_sym}
\title{Extract training fitted values from a psvr model}
\usage{
\method{fitted}{psvr_mape}(object, ...)

\method{fitted}{psvr_mape_sym}(object, ...)

\method{fitted}{psvr_rmspe}(object, ...)

\method{fitted}{psvr_rmspe_sym}(object, ...)
}
\arguments{
\item{object}{A fitted object of class \code{"psvr_mape"}, \code{"psvr_mape_sym"},
\code{"psvr_rmspe"} or \code{"psvr_rmspe_sym"}, from \code{\link[=psvr_mape]{psvr_mape()}}, \code{\link[=psvr_rmspe]{psvr_rmspe()}},
or a parsnip fit unwrapped with \code{\link[parsnip:reexports]{parsnip::extract_fit_engine()}}.}

\item{...}{Ignored.}
}
\value{
Numeric vector of length \code{N} (the number of training
observations), in training-row order.
}
\description{
Returns the length-\code{N} in-sample predictions \eqn{f(x_k)}{f(x_k)} recorded
when the model was fitted. No kernel matrix is rebuilt: the values are
recovered from state the solver already holds. For the MAPE models that is
a matvec against the retained \eqn{\Omega}{Omega}; for the LS-SVR models it
is the KKT stationarity identity
\deqn{f(x_k) = y_k - (10^{-6} + y_k^2/\Gamma)\,\alpha_k}{%
      f(x_k) = y_k - (1e-6 + y_k^2/Gamma) * alpha_k}
which costs \eqn{O(N)}{O(N)} and holds in both preconditioner branches.
The training inputs \code{X} are not retained for this purpose.
}
\details{
The result equals \code{predict(object, X_train)} to machine precision. It is
not bit-identical: the two use different summation orders (a BLAS matvec
versus the column-wise reduction in \code{predict()}), and for the LS-SVR
models the identity above is exact only up to the residual of the linear
solve. Observed agreement is within \code{3e-12} relative across the four
models.
}
\section{Not reachable through parsnip}{

parsnip registers neither \code{residuals.model_fit} nor \code{fitted.model_fit}, so
calling either generic on a \code{model_fit} dispatches to the stats default and
returns \code{NULL} \strong{silently} - no error, no warning. Reach the psvr object
first with \code{\link[parsnip:reexports]{parsnip::extract_fit_engine()}}, then call \code{fitted()} or
\code{residuals()} on that. psvr deliberately does not register S3 methods on
parsnip's class. Note also that \code{parsnip::augment()} recomputes predictions
on whatever \code{new_data} it is given and reports response residuals only.
}

\examples{
set.seed(1)
X <- matrix(runif(40, 0.5, 3), 20, 2)
y <- 2 + X[, 1]^2
fit <- psvr_rmspe(X, y, kernel = make_kernel("rbf"), gamma = 100)
head(fitted(fit))

# Through parsnip both generics return NULL on the model_fit wrapper;
# extract the engine object first.
df   <- data.frame(x1 = X[, 1], x2 = X[, 2], y = y)
spec <- psvr_rmspe_rbf(cost = 10, rbf_sigma = 0.8)
pfit <- parsnip::fit(spec, y ~ x1 + x2, data = df)
fitted(pfit)                                     # NULL
head(fitted(parsnip::extract_fit_engine(pfit)))  # the fitted values

}
\seealso{
\code{\link[=residuals.psvr_mape]{residuals.psvr_mape()}} and the other residuals methods
}
