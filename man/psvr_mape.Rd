% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psvr_mape.R
\name{psvr_mape}
\alias{psvr_mape}
\title{Fit an epsilon-SVR with MAPE loss}
\usage{
psvr_mape(
  X,
  y,
  sym_type = c("none", "even", "odd"),
  kernel,
  C,
  eps,
  solver = c("smo", "osqp"),
  tol = 0.001,
  max_iter = 100000L,
  alpha_init = NULL,
  alpha_star_init = NULL,
  warm_start_check = TRUE,
  block_k4_enabled = TRUE,
  engine = c("rcpp", "r"),
  ...,
  alpha_couple = 0.5,
  precomputed_Omega = NULL,
  precomputed_Omega_s = NULL
)
}
\arguments{
\item{X}{Numeric matrix of training inputs, one observation per row
(\eqn{N \times p}{N x p}).}

\item{y}{Numeric vector of training targets, length \eqn{N}{N}. Must satisfy
\eqn{y_k > 0}{y_k > 0} for every \eqn{k}{k}; percentage-error loss is
undefined otherwise, and this is checked rather than coerced.}

\item{sym_type}{Symmetry type, one of \code{"none"} (default), \code{"even"} or
\code{"odd"}. Maps onto the symmetry parameter \eqn{a}{a} of the paper:
\code{"none"} fits Model 1 and imposes no symmetry constraint; \code{"even"} sets
\eqn{a = +1}{a = +1}, enforcing \eqn{f(x) = f(-x)}{f(x) = f(-x)}; \code{"odd"}
sets \eqn{a = -1}{a = -1}, enforcing \eqn{f(x) = -f(-x)}{f(x) = -f(-x)}.
The fitted object stores the integer as \code{fit$a}. This is the same
vocabulary as the \code{sym_type} argument of the parsnip specifications, so
the two public surfaces agree. The symmetric variants require a kernel
satisfying Assumption 3 of the paper -- see \code{\link[=make_kernel]{make_kernel()}}.}

\item{kernel}{A kernel function created by \code{\link[=make_kernel]{make_kernel()}}.}

\item{C}{Regularization parameter, \eqn{C > 0}{C > 0}. Required.}

\item{eps}{Insensitivity tube half-width in percentage units,
\eqn{\epsilon \ge 0}{eps >= 0}. Required.}

\item{solver}{Backend for the dual quadratic program, \code{"smo"} (default) or
\code{"osqp"}. See the section above.}

\item{tol}{Numerical tolerance, default \code{1e-3}. \strong{Its meaning depends on
\code{solver}.} Under \code{"smo"} it is the convergence tolerance of the SMO loop,
and tighter values produce more iterations. Under \code{"osqp"} the solver runs
at its own fixed tolerances and \code{tol} is used only afterwards, as the
threshold below which a dual variable counts as zero when identifying free,
saturated and support-vector sets.}

\item{max_iter}{Maximum SMO iterations, default \code{100000L}. The solver emits a
\code{warning()} and returns \code{converged = FALSE} if it does not converge within
\code{max_iter}. Ignored for \code{solver = "osqp"}.}

\item{alpha_init, alpha_star_init}{Optional warm-start vectors for the SMO
solver, each a finite numeric vector of length \eqn{N}{N}. Projected onto
\eqn{\sum_k (\alpha_k - \alpha^*_k) = 0}{sum_k (alpha_k - alpha_star_k) = 0}
intersected with the per-sample box \eqn{[0, 100C/y_k]}{[0, 100*C/y_k]}
before the solve. \code{NULL} (default) cold-starts. \code{\link[=psvr_cv]{psvr_cv()}} manages these
automatically across folds.}

\item{warm_start_check}{Logical; if \code{TRUE} (default), validate
post-projection feasibility and \code{stop()} on violation. A surviving equality
residual is fatal rather than cosmetic: SMO conserves
\eqn{\sum_k (\alpha_k - \alpha^*_k)}{sum_k (alpha_k - alpha_star_k)}, so an
infeasible start is carried through to the returned solution.}

\item{block_k4_enabled}{Logical; if \code{TRUE} (default), enable the block-k=4
SMO inner loop. Each outer iteration may select a second working pair and
apply a two-dimensional joint update when the descent-guaranteed decoupling
criterion holds. \code{FALSE} restores the k=2 behaviour bit-identically.}

\item{engine}{One of \code{"rcpp"} (default) or \code{"r"}. Selects the SMO backend:
the C++ core, or the R reference implementation. Both produce
bit-identical results on the development toolchain; \code{"r"} is retained as
the reference and will be deprecated in 0.0.4.0 and removed in 0.1.0.}

\item{...}{Must be empty. Present only so that \code{alpha_couple},
\code{precomputed_Omega} and \code{precomputed_Omega_s} must be matched by their
exact names rather than by position or partial matching -- without it
\code{precomputed_Omega} would be a partial-match prefix of
\code{precomputed_Omega_s}. Passing anything here is an error, which is how a
mistyped argument name is caught.}

\item{alpha_couple}{Numeric in \eqn{[0, 1]}{[0, 1]}, default \code{0.5}. Coupling
penalty in the second-pair selection score
\eqn{\mathrm{gain} \times (1 - \alpha_{\mathrm{couple}} \cdot
  \mathrm{coupling})}{gain * (1 - alpha_couple * coupling)}. Exposed for
empirical tuning; rarely needs adjustment. Ignored when
\code{block_k4_enabled = FALSE}.}

\item{precomputed_Omega, precomputed_Omega_s}{INTERNAL -- used by \code{\link[=psvr_cv]{psvr_cv()}}
to share one full-dataset kernel matrix across folds. Users should not set
these. \code{precomputed_Omega} applies when \code{sym_type = "none"},
\code{precomputed_Omega_s} otherwise.}
}
\value{
For \code{sym_type = "none"}, an object of class \code{"psvr_mape"}: a list
with components \code{beta} (the pruned dual differences
\eqn{\beta = \alpha - \alpha^*}{beta = alpha - alpha_star} over the
support-vector indices, used by \code{predict()}), \code{alpha} and \code{alpha_star}
(the length-\eqn{N}{N} pre-pruning duals, retained for warm starts), \code{b},
\code{X_sv}, \code{y_sv}, \code{y_train}, \code{fitted_values}, \code{kernel}, \code{C}, \code{eps},
\code{n_train}, \code{p_train}, \code{iterations}, \code{converged} and \code{block_k4}.

For \code{sym_type = "even"} or \code{"odd"}, an object of class \code{"psvr_mape_sym"}:
the same components plus \code{a} (the symmetry parameter) and \code{spectral}
(adaptive spectral-shift diagnostics).

Methods are available for \code{\link[=predict]{predict()}}, \code{\link[=print]{print()}}, \code{\link[=coef]{coef()}}, \code{\link[=summary]{summary()}},
\code{\link[=fitted]{fitted()}} and \code{\link[=residuals]{residuals()}}.
}
\description{
Fits the percentage-error epsilon-SVR of the paper: Model 1 when
\code{sym_type = "none"}, and the symmetric-kernel Model 2 when \code{sym_type} is
\code{"even"} or \code{"odd"}. The dual is a quadratic program with the
sample-dependent box \eqn{|\beta_k| \le 100C/y_k}{|beta_k| <= 100*C/y_k}
and \eqn{\sum_k \beta_k = 0}{sum_k beta_k = 0}, solved by the built-in SMO
loop or by \code{osqp}.
}
\details{
For the least-squares / RMSPE family (Models 3 and 4) see \code{\link[=psvr_rmspe]{psvr_rmspe()}}.
The two are deliberately separate functions: they share no solver, no dual
structure and no hyperparameter search space, so a single signature would
make most of its own arguments conditional. The name \code{psvr()} is reserved
for a future automatic-selection front end and is \strong{not} a synonym for
either.
}
\section{Choosing a solver}{

\code{solver = "smo"} (the default) uses the built-in sequential minimal
optimisation loop. It does \strong{not} reliably converge within \code{max_iter} on
linear and polynomial kernels; \code{converged} is \code{FALSE} and \code{iterations}
reaches the cap. The RBF kernel is unaffected. Prefer \code{solver = "osqp"}
for linear and polynomial kernels when accuracy matters.
}

\examples{
set.seed(1)
X <- matrix(rnorm(40), 20, 2)
y <- rlnorm(20)
K <- make_kernel("rbf", sigma = 1)

fit <- psvr_mape(X, y, kernel = K, C = 10, eps = 5)
predict(fit, X[1:3, , drop = FALSE])

# Even-symmetric variant (Model 2): f(x) = f(-x).
fit_sym <- psvr_mape(X, y, sym_type = "even", kernel = K, C = 10, eps = 5)
predict(fit_sym, X[1:3, , drop = FALSE])

}
\seealso{
\code{\link[=psvr_rmspe]{psvr_rmspe()}} for the LS-SVR / RMSPE family, \code{\link[=psvr_cv]{psvr_cv()}} for
cross-validation with warm-start carryover, \code{\link[=make_kernel]{make_kernel()}} for kernels.
}
