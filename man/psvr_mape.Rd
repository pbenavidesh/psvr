% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip.R
\name{psvr_mape}
\alias{psvr_mape}
\title{Parsnip model spec: epsilon-SVR with MAPE loss (Model 1)}
\usage{
psvr_mape(mode = "regression", engine = "psvr", cost = NULL, svm_margin = NULL)
}
\arguments{
\item{mode}{Only \code{"regression"} is supported.}

\item{engine}{Only \code{"psvr"} is available.}

\item{cost}{Regularization parameter \code{C > 0}.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.}

\item{svm_margin}{Epsilon tube half-width \verb{ε ≥ 0} (percentage units).
Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.}
}
\value{
A \code{psvr_mape_model} / \code{model_spec} object.
}
\description{
Creates a parsnip model specification for \code{\link[=mape_svr]{mape_svr()}}.  The kernel and
solver tolerance are engine arguments passed via \code{set_engine()}.
}
\examples{
\dontrun{
library(parsnip)
K <- make_kernel("rbf", sigma = 1)
spec <- psvr_mape(cost = 10, svm_margin = 5) |>
  set_engine("psvr", kernel = K)
fit(spec, mpg ~ ., data = mtcars)
}

}
