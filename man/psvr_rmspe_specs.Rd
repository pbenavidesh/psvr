% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip.R
\name{psvr_rmspe_specs}
\alias{psvr_rmspe_specs}
\alias{psvr_rmspe_rbf}
\alias{psvr_rmspe_poly}
\alias{psvr_rmspe_linear}
\title{Parsnip model specs: LS-SVR with RMSPE loss (Model 3)}
\usage{
psvr_rmspe_rbf(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  rbf_sigma = NULL,
  sym_type = NULL
)

psvr_rmspe_poly(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  degree = NULL,
  scale_factor = NULL,
  sym_type = NULL
)

psvr_rmspe_linear(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  sym_type = NULL
)
}
\arguments{
\item{mode}{Only \code{"regression"} is supported.}

\item{engine}{Only \code{"psvr"} is available.}

\item{cost}{Regularization parameter \eqn{\Gamma > 0}{Gamma > 0}.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
Mapped to \code{\link[=cost_psvr]{cost_psvr()}}, whose default range \verb{[-2, 10]} on the log2 scale
(\eqn{\Gamma \le 1024}{Gamma <= 1024}) is the
\eqn{\epsilon}{epsilon}-SVR range and is \strong{too narrow for LS-SVR}.  On
Boston Housing the tuned optimum is around
\eqn{1.1 \times 10^5}{1.1 x 10^5} — roughly 100 times that ceiling — so a
grid over the default is boundary-trapped.  Pass \code{\link[=cost_psvr_ls_data]{cost_psvr_ls_data()}}
built from the training outcome explicitly, via \code{update()} on the
extracted parameter set or via \code{\link[=psvr_option_add_cost_ls]{psvr_option_add_cost_ls()}} for a workflow
set.  This \strong{cannot} be automated: \code{tune} finalizes parameters from the
molded \strong{predictors} only and never passes the outcome to
\code{dials::finalize()}, so no \code{finalize} function on \code{cost} could compute it.}

\item{rbf_sigma}{RBF bandwidth \eqn{\sigma > 0}{sigma > 0}.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
Mapped to \code{\link[=rbf_sigma_psvr]{rbf_sigma_psvr()}}, whose default range \verb{[-3, 1]} on the log10
scale is a fixed, conservative fallback.  \strong{The range does not finalize
automatically from the training data.}  \code{\link[=rbf_sigma_psvr]{rbf_sigma_psvr()}} sets
\code{finalize = NULL}, so \code{dials::finalize()} leaves it untouched.  To centre
the range on the data, pass \code{\link[=rbf_sigma_psvr_data]{rbf_sigma_psvr_data()}} computed on the
\strong{preprocessed} predictors explicitly — via \code{update()} on the extracted
parameter set, or via \code{\link[=psvr_option_add]{psvr_option_add()}} for a workflow set.
(RBF specs only.)}

\item{sym_type}{Symmetry type: \code{"none"} (default) fits the non-symmetric
LS-SVR of Model 3; \code{"even"} (a = 1) and \code{"odd"} (a = -1) fit the
symmetric LS-SVR of Model 4.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimise over the levels
during CV; see \code{\link[=sym_type_param]{sym_type_param()}} to restrict which levels are searched.}

\item{degree}{Polynomial degree \eqn{\ge 1}{>= 1}.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
(Polynomial specs only.)}

\item{scale_factor}{Polynomial constant term (\code{coef0}).  Use \code{\link[hardhat:tune]{hardhat::tune()}} to
optimize.  (Polynomial specs only.)}
}
\value{
A parsnip \code{model_spec} object of the corresponding class.
}
\description{
Create parsnip model specifications for \code{\link[=psvr_rmspe]{psvr_rmspe()}} with a fixed kernel
type.  \code{cost} maps to the regularization parameter \eqn{\Gamma}{Gamma}.
}
\section{Engine arguments}{

The \code{precondition} argument of \code{\link[=psvr_rmspe]{psvr_rmspe()}} is exposed as a non-tunable
engine argument. Pass it via \code{\link[parsnip:set_engine]{parsnip::set_engine()}}, e.g.
\code{set_engine("psvr", precondition = "always")}. Default is \code{"auto"}. See
\code{\link[=psvr_rmspe]{psvr_rmspe()}} for accepted values and semantics.
}

\examples{
\dontrun{
library(parsnip)
spec <- psvr_rmspe_rbf(cost = 1000, rbf_sigma = 1) |>
  set_engine("psvr")

spec_poly <- psvr_rmspe_poly(cost = 1000, degree = 2, scale_factor = 1) |>
  set_engine("psvr")

spec_lin <- psvr_rmspe_linear(cost = 1000) |>
  set_engine("psvr")

# Symmetric LS-SVR (Model 4) via the sym_type argument:
spec_sym <- psvr_rmspe_rbf(cost = 1000, rbf_sigma = 1,
                           sym_type = "even") |>
  set_engine("psvr")
}

}
