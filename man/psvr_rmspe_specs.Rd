% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip.R
\name{psvr_rmspe_specs}
\alias{psvr_rmspe_specs}
\alias{psvr_rmspe_rbf}
\alias{psvr_rmspe_poly}
\alias{psvr_rmspe_linear}
\title{Parsnip model specs: LS-SVR with RMSPE loss (Model 3)}
\usage{
psvr_rmspe_rbf(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  rbf_sigma = NULL
)

psvr_rmspe_poly(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  degree = NULL,
  scale_factor = NULL
)

psvr_rmspe_linear(mode = "regression", engine = "psvr", cost = NULL)
}
\arguments{
\item{mode}{Only \code{"regression"} is supported.}

\item{engine}{Only \code{"psvr"} is available.}

\item{cost}{Regularization parameter \code{Γ > 0}.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.}

\item{rbf_sigma}{RBF bandwidth σ > 0.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
(RBF specs only.)}

\item{degree}{Polynomial degree ≥ 1.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
(Polynomial specs only.)}

\item{scale_factor}{Polynomial constant term (coef₀).  Use \code{\link[hardhat:tune]{hardhat::tune()}} to
optimize.  (Polynomial specs only.)}
}
\value{
A parsnip \code{model_spec} object of the corresponding class.
}
\description{
Create parsnip model specifications for \code{\link[=rmspe_lssvr]{rmspe_lssvr()}} with a fixed kernel
type.  \code{cost} maps to the regularization parameter \code{Γ}.
}
\examples{
\dontrun{
library(parsnip)
spec <- psvr_rmspe_rbf(cost = 1000, rbf_sigma = 1) |>
  set_engine("psvr")

spec_poly <- psvr_rmspe_poly(cost = 1000, degree = 2, scale_factor = 1) |>
  set_engine("psvr")

spec_lin <- psvr_rmspe_linear(cost = 1000) |>
  set_engine("psvr")
}

}
