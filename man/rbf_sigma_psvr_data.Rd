% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{rbf_sigma_psvr_data}
\alias{rbf_sigma_psvr_data}
\title{RBF sigma parameter with data-driven range for psvr models}
\usage{
rbf_sigma_psvr_data(
  X,
  width = 10,
  sample_size = 500L,
  seed = NULL,
  trans = scales::log10_trans()
)
}
\arguments{
\item{X}{A numeric matrix or data frame of predictors (already
preprocessed — centred, scaled, etc.).}

\item{width}{Positive scalar. Multiplier that sets the half-width of the
search range around the heuristic sigma. Default \code{10} (one decade).}

\item{sample_size}{Integer. Passed to \code{\link[=sigma_heuristic]{sigma_heuristic()}}. Default \code{500L}.}

\item{seed}{Integer seed for subsampling. Passed to \code{\link[=sigma_heuristic]{sigma_heuristic()}}.
Default \code{NULL}.}

\item{trans}{A \code{scales} transformation object. Default \code{scales::log10_trans()}.}
}
\value{
A \code{quant_param} dials object with a data-driven search range.
}
\description{
A convenience wrapper that combines \code{\link[=sigma_heuristic]{sigma_heuristic()}} and
\code{\link[=rbf_sigma_psvr]{rbf_sigma_psvr()}} in a single call. It computes the median pairwise
distance from \code{X} and returns a \code{quant_param} whose search range spans
one order of magnitude either side of the heuristic value on the log10
scale (i.e., \verb{[log10(sigma_med / width), log10(sigma_med * width)]}).
}
\examples{
X <- matrix(rnorm(200), ncol = 4)
rbf_sigma_psvr_data(X)
rbf_sigma_psvr_data(X, width = 5)

}
\seealso{
\code{\link[=sigma_heuristic]{sigma_heuristic()}}, \code{\link[=rbf_sigma_psvr]{rbf_sigma_psvr()}}
}
