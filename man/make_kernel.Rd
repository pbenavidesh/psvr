% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel.R
\name{make_kernel}
\alias{make_kernel}
\title{Create a kernel function}
\usage{
make_kernel(
  type = c("rbf", "linear", "polynomial"),
  sigma = 1,
  degree = 3L,
  coef0 = 1
)
}
\arguments{
\item{type}{Kernel type: \code{"rbf"}, \code{"linear"}, or \code{"polynomial"}.}

\item{sigma}{Bandwidth for the RBF kernel, \code{sigma > 0} (default \code{1}).}

\item{degree}{Integer degree for the polynomial kernel, \code{degree >= 1}
(default \code{3}).}

\item{coef0}{Constant term for the polynomial kernel (default \code{1}).}
}
\value{
A function \code{K(xi, xj)} where \code{xi} and \code{xj} are numeric vectors of
the same length, returning a scalar kernel evaluation.
}
\description{
Returns a closure \code{K(xi, xj)} suitable for use with all four psvr model
fitting functions. The returned function accepts numeric vectors of any sign,
which is required by the symmetric models (Models 2 and 4) that evaluate
\code{K(xk, -xl)}.
}
\details{
The three supported kernels are:
\itemize{
\item \strong{RBF:} \verb{K(xi, xj) = exp(-‖xi - xj‖² / (2 * sigma²))}
\item \strong{Linear:} \verb{K(xi, xj) = xi · xj}
\item \strong{Polynomial:} \verb{K(xi, xj) = (xi · xj + coef0)^degree}
}

RBF and even-degree polynomial kernels satisfy Assumption 3 of the paper
(kernel symmetry), making them compatible with the symmetric models.
The linear kernel and odd-degree polynomial kernels do \strong{not} satisfy
Assumption 3 and should not be used with \code{mape_sym_svr()} or
\code{rmspe_sym_lssvr()}.
}
\examples{
K <- make_kernel("rbf", sigma = 0.5)
K(c(1, 2), c(3, 4))

K_lin <- make_kernel("linear")
K_lin(c(1, 0), c(0, 1))

K_poly <- make_kernel("polynomial", degree = 2, coef0 = 1)
K_poly(c(1, 2), c(3, 4))

}
