% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip.R
\name{psvr_mape_specs}
\alias{psvr_mape_specs}
\alias{psvr_mape_rbf}
\alias{psvr_mape_poly}
\alias{psvr_mape_linear}
\title{Parsnip model specs: epsilon-SVR with MAPE loss (Model 1)}
\usage{
psvr_mape_rbf(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  margin = NULL,
  rbf_sigma = NULL,
  sym_type = NULL
)

psvr_mape_poly(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  margin = NULL,
  degree = NULL,
  scale_factor = NULL,
  sym_type = NULL
)

psvr_mape_linear(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  margin = NULL,
  sym_type = NULL
)
}
\arguments{
\item{mode}{Only \code{"regression"} is supported.}

\item{engine}{Only \code{"psvr"} is available.}

\item{cost}{Regularization parameter \code{C > 0}.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
Mapped to \code{\link[=cost_psvr]{cost_psvr()}}, whose default range is \verb{[-2, 10]} on the log2
scale (about 0.25 to 1024).  That range is adequate here, where typical
\eqn{\epsilon}{epsilon}-SVR optima lie in [10, 100].  The LS-SVR specs
(\link{psvr_rmspe_specs}) map \code{cost} to \eqn{\Gamma}{Gamma} and need a much
wider range; see there.}

\item{margin}{Epsilon tube half-width \eqn{\epsilon \ge 0}{epsilon >= 0} expressed
as a percentage
of each target value.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.  Mapped to
\code{\link[=margin_percentage]{margin_percentage()}} with default range \verb{[1, 20]} (percentage units).
Named to match \code{parsnip::svm_rbf()}; note the units differ from
\code{dials::svm_margin()}, which is absolute rather than percentage.}

\item{rbf_sigma}{RBF bandwidth \eqn{\sigma > 0}{sigma > 0}.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
Mapped to \code{\link[=rbf_sigma_psvr]{rbf_sigma_psvr()}}, whose default range \verb{[-3, 1]} on the log10
scale is a fixed, conservative fallback.  \strong{The range does not finalize
automatically from the training data.}  \code{\link[=rbf_sigma_psvr]{rbf_sigma_psvr()}} sets
\code{finalize = NULL}, so \code{dials::finalize()} leaves it untouched.  To centre
the range on the data, pass \code{\link[=rbf_sigma_psvr_data]{rbf_sigma_psvr_data()}} computed on the
\strong{preprocessed} predictors explicitly — via \code{update()} on the extracted
parameter set, or via \code{\link[=psvr_option_add]{psvr_option_add()}} for a workflow set.
(RBF specs only.)}

\item{sym_type}{Symmetry type: \code{"none"} (default) fits the non-symmetric
\eqn{\epsilon}{epsilon}-SVR of Model 1; \code{"even"} (a = 1) and \code{"odd"}
(a = -1) fit the symmetric \eqn{\epsilon}{epsilon}-SVR of Model 2.
Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimise over the levels
during CV; see \code{\link[=sym_type_param]{sym_type_param()}} to restrict which levels are searched.}

\item{degree}{Polynomial degree \eqn{\ge 1}{>= 1}.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
(Polynomial specs only.)}

\item{scale_factor}{Polynomial constant term (\code{coef0}).  Use \code{\link[hardhat:tune]{hardhat::tune()}} to
optimize.  (Polynomial specs only.)}
}
\value{
A parsnip \code{model_spec} object of the corresponding class.
}
\description{
Create parsnip model specifications for \code{\link[=psvr_mape]{psvr_mape()}} with a fixed kernel
type.  Kernel parameters are tunable parsnip arguments; the symmetry
parameter \code{a} and solver tolerance are engine arguments passed via
\code{set_engine()}.
}
\examples{
\dontrun{
library(parsnip)
spec <- psvr_mape_rbf(cost = 10, margin = 1, rbf_sigma = 1) |>
  set_engine("psvr")

spec_poly <- psvr_mape_poly(cost = 10, margin = 1, degree = 2,
                            scale_factor = 1) |>
  set_engine("psvr")

spec_lin <- psvr_mape_linear(cost = 10, margin = 1) |>
  set_engine("psvr")

# Symmetric epsilon-SVR (Model 2) via the sym_type argument:
spec_sym <- psvr_mape_rbf(cost = 10, margin = 1, rbf_sigma = 1,
                          sym_type = "even") |>
  set_engine("psvr")
}

}
