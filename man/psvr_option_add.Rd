% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{psvr_option_add}
\alias{psvr_option_add}
\title{Apply data-driven rbf_sigma to all psvr workflows in a workflow set}
\usage{
psvr_option_add(wf_set, X, width = 10, sample_size = 500L, seed = NULL)
}
\arguments{
\item{wf_set}{A \code{workflow_set} object.}

\item{X}{A numeric matrix or data frame of preprocessed predictors.}

\item{width}{Positive scalar. Passed to \code{\link[=rbf_sigma_psvr_data]{rbf_sigma_psvr_data()}}.
Default \code{10}.}

\item{sample_size}{Integer. Passed to \code{\link[=sigma_heuristic]{sigma_heuristic()}}. Default \code{500L}.}

\item{seed}{Integer seed for subsampling. Default \code{NULL}.}
}
\value{
The updated \code{workflow_set} (the same object with \code{option_add()}
applied to each psvr workflow).
}
\description{
A convenience wrapper that calls \code{\link[workflowsets:option_add]{workflowsets::option_add()}} for every
psvr workflow in \code{wf_set} (those whose \code{wflow_id} contains \code{"m1"},
\code{"m2"}, \code{"m3"}, or \code{"m4"}), replacing the \code{rbf_sigma} dials parameter
with a data-driven one built from \code{X} via \code{\link[=rbf_sigma_psvr_data]{rbf_sigma_psvr_data()}}.
}
\examples{
\dontrun{
# After building wf_set and preprocessing:
train_baked <- rec |> prep() |> bake(new_data = train)
wf_set <- psvr_option_add(wf_set, train_baked |> select(-outcome))
}

}
\seealso{
\code{\link[=rbf_sigma_psvr_data]{rbf_sigma_psvr_data()}}, \code{\link[=sigma_heuristic]{sigma_heuristic()}}
}
