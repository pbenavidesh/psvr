% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsnip.R
\name{psvr_rmspe_sym_specs}
\alias{psvr_rmspe_sym_specs}
\alias{psvr_rmspe_sym_rbf}
\alias{psvr_rmspe_sym_poly}
\alias{psvr_rmspe_sym_linear}
\title{Parsnip model specs: symmetric LS-SVR with RMSPE loss (Model 4)}
\usage{
psvr_rmspe_sym_rbf(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  rbf_sigma = NULL,
  sym_type = NULL
)

psvr_rmspe_sym_poly(
  mode = "regression",
  engine = "psvr",
  cost = NULL,
  degree = NULL,
  scale_factor = NULL
)

psvr_rmspe_sym_linear(mode = "regression", engine = "psvr", cost = NULL)
}
\arguments{
\item{mode}{Only \code{"regression"} is supported.}

\item{engine}{Only \code{"psvr"} is available.}

\item{cost}{Regularization parameter \code{Γ > 0}.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
Mapped to \code{\link[=cost_psvr]{cost_psvr()}} with range \verb{[-2, 10]} on the log2 scale — wider
than \code{dials::cost()} to cover the larger values needed by LS-SVR models.}

\item{rbf_sigma}{RBF bandwidth σ > 0.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
Mapped to \code{\link[=rbf_sigma_psvr]{rbf_sigma_psvr()}}; the search range auto-finalizes using the
median-distance heuristic when training data are available.
(RBF specs only.)}

\item{sym_type}{Symmetry type: \code{"even"} (default, a = 1) or \code{"odd"}
(a = -1).  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimise over both values during CV.}

\item{degree}{Polynomial degree ≥ 1.  Use \code{\link[hardhat:tune]{hardhat::tune()}} to optimize.
(Polynomial specs only.)}

\item{scale_factor}{Polynomial constant term (coef₀).  Use \code{\link[hardhat:tune]{hardhat::tune()}} to
optimize.  (Polynomial specs only.)}
}
\value{
A parsnip \code{model_spec} object of the corresponding class.
}
\description{
Create parsnip model specifications for \code{\link[=rmspe_sym_lssvr]{rmspe_sym_lssvr()}} with a fixed
kernel type.  The symmetry type is exposed as the tunable \code{sym_type}
argument (\code{"even"} for a = 1, \code{"odd"} for a = -1); pass
\code{sym_type = tune()} to let CV select it automatically.
}
\section{Engine arguments}{

The \code{precondition} argument of \code{\link[=rmspe_sym_lssvr]{rmspe_sym_lssvr()}} is exposed as a
non-tunable engine argument. Pass it via \code{\link[parsnip:set_engine]{parsnip::set_engine()}}, e.g.
\code{set_engine("psvr", precondition = "always")}. Default is \code{"auto"}. See
\code{\link[=rmspe_sym_lssvr]{rmspe_sym_lssvr()}} for accepted values and semantics.
}

\examples{
\dontrun{
library(parsnip)
spec <- psvr_rmspe_sym_rbf(cost = 1000, rbf_sigma = 1) |>
  set_engine("psvr")

spec_poly <- psvr_rmspe_sym_poly(cost = 1000, degree = 2,
                                 scale_factor = 1) |>
  set_engine("psvr")

spec_lin <- psvr_rmspe_sym_linear(cost = 1000) |>
  set_engine("psvr")
}

}
