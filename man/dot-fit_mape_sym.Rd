% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mape_sym_svr.R
\name{.fit_mape_sym}
\alias{.fit_mape_sym}
\title{Fit symmetric epsilon-SVR with MAPE loss (Model 2) — internal}
\usage{
.fit_mape_sym(
  X,
  y,
  kernel,
  C,
  eps,
  a = 1,
  solver = c("smo", "osqp"),
  tol = 0.001,
  max_iter = 100000L,
  alpha_init = NULL,
  alpha_star_init = NULL,
  warm_start_check = TRUE,
  precomputed_Omega_s = NULL,
  block_k4_enabled = TRUE,
  alpha_couple = 0.5,
  engine = c("rcpp", "r")
)
}
\arguments{
\item{X, y, kernel, C, eps, solver, tol}{See \code{\link[=psvr_mape]{psvr_mape()}}.}

\item{a}{Symmetry type: \code{1} (even) or \code{-1} (odd). This is the internal
integer; the public argument is \code{sym_type}, with \code{"even"} mapping to
\code{a = 1} and \code{"odd"} to \code{a = -1}. Neither \code{\link[=psvr_mape]{psvr_mape()}} nor the parsnip
specifications expose \code{a} directly.}

\item{alpha_init, alpha_star_init}{Optional length-N numeric vectors of
starting values for the two sets of SMO dual variables, typically the
converged duals of a previous fit on overlapping data. They are projected
back onto the constraint set before the solve (the warm-start procedure of
Theorem 5 / Algorithm 1 in arXiv:2605.01446 v3). \code{NULL} cold-starts.}

\item{warm_start_check}{Logical; if \code{TRUE}, validate the post-projection
feasibility of the warm-start vectors. Default \code{TRUE}.}
}
\value{
A list of class \code{"psvr_mape_sym"} (legacy shape).
}
\description{
Internal fitter for the symmetric MAPE epsilon-SVR family. Use \code{\link[=psvr_mape]{psvr_mape()}}
with \code{sym_type = "even"} / \code{"odd"} instead. Returns the \code{psvr_mape_sym}
shape, which is what \code{\link[=psvr_mape]{psvr_mape()}} and the parsnip engine fit wrappers both
return with \code{sym_type = "even"} / \code{"odd"}. The kernel must satisfy
Assumption 3 of the paper; see \code{\link[=make_kernel]{make_kernel()}}.
}
\keyword{internal}
