% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mape_sym_svr.R
\name{.fit_mape_sym}
\alias{.fit_mape_sym}
\title{Fit symmetric epsilon-SVR with MAPE loss (Model 2) — internal}
\usage{
.fit_mape_sym(
  X,
  y,
  kernel,
  C,
  eps,
  a = 1,
  solver = c("smo", "osqp"),
  tol = 1e-05,
  alpha_init = NULL,
  alpha_star_init = NULL,
  warm_start_check = TRUE,
  new_mask = NULL,
  precomputed_Omega_s = NULL,
  block_k4_enabled = TRUE,
  alpha_couple = 0.5,
  engine = c("rcpp", "r")
)
}
\arguments{
\item{X, y, kernel, C, eps, a, solver, tol}{See \code{\link[=mape_sym_svr]{mape_sym_svr()}}.}

\item{alpha_init, alpha_star_init}{Optional length-N numeric warm-start
vectors (Theorem 5); \code{NULL} cold-starts.}

\item{warm_start_check}{Logical; if \code{TRUE}, validate the post-projection
feasibility of the warm-start vectors. Default \code{TRUE}.}

\item{new_mask}{Optional logical vector (length N) flagging samples that
are NEW relative to the previous fit (used to distribute the equality-
constraint projection over new samples only). \code{NULL} infers
"new = both alpha and alpha_star are exactly zero".}
}
\value{
A list of class \code{"psvr_mape_sym"} (legacy shape).
}
\description{
Internal fitter for the symmetric MAPE epsilon-SVR family. Use \code{\link[=psvr]{psvr()}}
with \code{loss = "mape"} and \code{sym = +1L} / \code{-1L} instead. Returns the legacy
\code{psvr_mape_sym} shape; the deprecation wrapper \code{\link[=mape_sym_svr]{mape_sym_svr()}} forwards
directly to this function. The kernel must satisfy Assumption 3 of the
paper; see \code{\link[=make_kernel]{make_kernel()}}.
}
\keyword{internal}
