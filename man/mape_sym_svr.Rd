% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mape_sym_svr.R
\name{mape_sym_svr}
\alias{mape_sym_svr}
\title{Fit symmetric epsilon-SVR with MAPE loss (Model 2)}
\usage{
mape_sym_svr(
  X,
  y,
  kernel,
  C,
  eps,
  a = 1,
  solver = c("smo", "osqp"),
  tol = 1e-05
)
}
\arguments{
\item{X}{Numeric matrix of training inputs, one observation per row (N × p).}

\item{y}{Numeric vector of training targets, length N. Must satisfy \code{y > 0}.}

\item{kernel}{A kernel function created by \code{\link[=make_kernel]{make_kernel()}}.}

\item{C}{Regularization parameter \code{C > 0}.}

\item{eps}{Insensitivity tube half-width \verb{ε ≥ 0} (in percentage units).}

\item{a}{Symmetry parameter: \code{1} for even symmetry \code{f(x) = f(-x)},
\code{-1} for odd symmetry \code{f(x) = -f(-x)}.}

\item{solver}{Backend that solves the dual QP.  \code{"smo"} (default) uses an
internal libsvm-style SMO solver with no third-party dependency; \code{"osqp"}
delegates to the \code{osqp} package.  Both backends solve the same QP and
return the same support vectors and bias up to numerical tolerance.}

\item{tol}{Threshold below which \verb{|βk|} is treated as zero (default \code{1e-5}).}
}
\value{
An object of class \code{"psvr_mape_sym"}, a list with components:
\describe{
\item{\code{beta}}{Numeric vector of non-zero dual differences \code{βk}.}
\item{\code{b}}{Numeric scalar bias term.}
\item{\code{X_sv}}{Numeric matrix of support vector inputs.}
\item{\code{y_sv}}{Numeric vector of support vector targets.}
\item{\code{kernel}}{The kernel function used.}
\item{\code{C}}{The regularization parameter \code{C}.}
\item{\code{eps}}{The \code{ε} value used.}
\item{\code{a}}{The symmetry parameter.}
\item{\code{n_train}}{Number of training observations.}
\item{\code{p_train}}{Number of training features (columns).}
}
}
\description{
Solves the quadratic program derived in Theorem 2 of Benavides-Herrera et al.
(2026) via \code{osqp}. The symmetry constraint \verb{f(x) = a·f(-x)} is enforced by
the symmetric kernel matrix \verb{Ωs = ½(Ω + a·Ω*)} (with the \verb{½} from the
representer theorem baked in), built via \code{\link[=sym_kernel_matrix]{sym_kernel_matrix()}}.
}
\details{
The dual in variables \verb{u = [α; α*] ∈ R^\{2N\}} is:
\itemize{
\item \strong{P} = \verb{[Ωs, -Ωs; -Ωs, Ωs]} so that osqp's \verb{½ uᵀPu} evaluates to
\verb{½ βᵀΩsβ = ¼ βᵀKsβ}, matching the \verb{−¼} coefficient in Theorem 2.
\item \strong{q} = \verb{[y(ε/100 − 1); y(1 + ε/100)]} (identical to Model 1)
\item \strong{Equality:} \verb{[1ᵀ, −1ᵀ] u = 0}
\item \strong{Box:} \verb{0 ≤ αk ≤ 100C/yk}, \verb{0 ≤ αk* ≤ 100C/yk} (identical to Model 1)
}

This convention matches \code{\link[=rmspe_sym_lssvr]{rmspe_sym_lssvr()}} (Model 4): both pass \code{Ωs}
(with \verb{½} already absorbed) into the solver.

The kernel must satisfy Assumption 3 of the paper; see \code{\link[=make_kernel]{make_kernel()}}.
}
\examples{
X <- matrix(1:6, ncol = 2)
y <- c(2.1, 3.8, 6.2)
K <- make_kernel("rbf", sigma = 1)
fit <- mape_sym_svr(X, y, kernel = K, C = 10, eps = 5, a = 1)
predict(fit, X)

}
