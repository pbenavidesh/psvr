% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel-accessor.R
\name{.make_kernel_accessor}
\alias{.make_kernel_accessor}
\title{Build a kernel-matrix accessor (internal)}
\usage{
.make_kernel_accessor(Omega)
}
\arguments{
\item{Omega}{Kernel matrix (\verb{N x N}), already with any required jitter
and symmetrisation applied by the caller.}
}
\value{
A list with components:
\describe{
\item{\code{get_column(p)}}{Returns column \code{p} of \code{Omega} (length-\code{N}
numeric vector).}
\item{\code{get_diag()}}{Returns \code{diag(Omega)} (length-\code{N} numeric
vector). Cached at construction.}
\item{\code{get_entry(p, q)}}{Returns the scalar \code{Omega[p, q]}.}
\item{\code{get_matvec(v)}}{Returns the matrix-vector product
\code{as.numeric(Omega \%*\% v)} (length-\code{N}). Used for one-shot
gradient refreshes; preserves BLAS efficiency.}
\item{\code{n}}{Integer, equal to \code{nrow(Omega)}.}
}
}
\description{
Wraps a fully-materialised kernel matrix in a list of accessor closures.
The SMO solver and other iterative consumers read kernel values through
this interface so that future phases can replace the underlying
representation (e.g., the F6 LRU cache backed by Rcpp) without touching
the consumers.
}
\details{
This F2 implementation is a thin wrapper over the materialised matrix:
every closure delegates directly to base-R indexing or BLAS. The
diagonal is computed once at construction (\code{diag()} is called a single
time) and reused on every \code{get_diag()} call.

\strong{Symmetry assumption.} The wrapped matrix is assumed symmetric. Both
\eqn{\Omega} (a kernel matrix for symmetric \code{K}) and \eqn{\Omega_s =
\tfrac{1}{2}(\Omega + a\,\Omega^*)} (used by the symmetric models when
\code{K} satisfies Assumption 3) are symmetric throughout this package, so
callers may use \code{get_column(p)[k]} in place of a row read
\code{Omega[p, k]}. The SMO solver relies on this in its WSS3 step.
}
\keyword{internal}
