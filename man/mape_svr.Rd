% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mape_svr.R
\name{mape_svr}
\alias{mape_svr}
\title{Fit epsilon-SVR with MAPE loss (Model 1)}
\usage{
mape_svr(X, y, kernel, C, eps, tol = 1e-05)
}
\arguments{
\item{X}{Numeric matrix of training inputs, one observation per row (N × p).}

\item{y}{Numeric vector of training targets, length N. Must satisfy \code{y > 0}.}

\item{kernel}{A kernel function created by \code{\link[=make_kernel]{make_kernel()}}.}

\item{C}{Regularization parameter \code{C > 0}.}

\item{eps}{Insensitivity tube half-width \verb{ε ≥ 0} (in percentage units,
i.e., the tube is \verb{ε\%} of each target).}

\item{tol}{Threshold below which \verb{|βk|} is treated as zero when selecting
support vectors and free support vectors (default \code{1e-5}).}
}
\value{
An object of class \code{"psvr_mape"}, a list with components:
\describe{
\item{\code{beta}}{Numeric vector of non-zero dual differences \code{βk} (length
equal to the number of support vectors).}
\item{\code{b}}{Numeric scalar bias term.}
\item{\code{X_sv}}{Numeric matrix of support vector inputs.}
\item{\code{y_sv}}{Numeric vector of support vector targets.}
\item{\code{kernel}}{The kernel function used.}
\item{\code{C}}{The regularization parameter \code{C}.}
\item{\code{eps}}{The \code{ε} value used.}
\item{\code{n_train}}{Number of training observations.}
\item{\code{p_train}}{Number of training features (columns).}
}
}
\description{
Solves the quadratic program derived in Theorem 1 of Benavides-Herrera et al.
(2026) via \code{osqp}. The dual is expressed in variables
\verb{u = [α; α*] ∈ R^\{2N\}} with:
}
\details{
\itemize{
\item \strong{P} = \verb{[Ω, -Ω; -Ω, Ω]} (2N × 2N, upper triangular passed to osqp)
\item \strong{q} = \verb{[y(ε/100 − 1); y(1 + ε/100)]}
\item \strong{Equality:} \verb{[1ᵀ, −1ᵀ] u = 0}
\item \strong{Box:} \verb{0 ≤ αk ≤ 100C/yk}, \verb{0 ≤ αk* ≤ 100C/yk}
}

After solving, \verb{βk = αk − αk*} and only support vectors with \verb{|βk| > tol}
are retained.
}
\examples{
X <- matrix(1:6, ncol = 2)
y <- c(2.1, 3.8, 6.2)
K <- make_kernel("rbf", sigma = 1)
fit <- mape_svr(X, y, kernel = K, C = 10, eps = 5)
predict(fit, X)

}
