% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{mape_svr}
\alias{mape_svr}
\title{Fit epsilon-SVR with MAPE loss (deprecated)}
\usage{
mape_svr(X, y, kernel, C, eps, solver = c("smo", "osqp"), tol = 1e-05)
}
\arguments{
\item{X, y}{Training matrix and strictly-positive target vector.}

\item{kernel}{Kernel closure from \code{\link[=make_kernel]{make_kernel()}}.}

\item{C}{Regularization parameter \code{C > 0}.}

\item{eps}{Insensitivity tube half-width (\% units), \code{eps >= 0}.}

\item{solver}{Backend: \code{"smo"} (default) or \code{"osqp"}.}

\item{tol}{Solver zero-threshold.}
}
\value{
A list of class \code{"psvr_mape"}.
}
\description{
Soft-deprecated wrapper retained for backwards compatibility. New code
should use \code{psvr(loss = "mape", ...)}. Returns the legacy \code{psvr_mape}
object shape; the legacy \code{predict.psvr_mape()} / \code{print.psvr_mape()} /
\code{coef.psvr_mape()} methods continue to dispatch correctly.
}
\examples{
\dontrun{
# Use psvr() instead:
fit <- psvr(X, y, loss = "mape", kernel = make_kernel("rbf"),
            C = 10, eps = 5)
}

}
\keyword{internal}
