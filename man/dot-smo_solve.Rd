% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smo_solve.R
\name{.smo_solve}
\alias{.smo_solve}
\title{Dispatcher: SMO solver with engine choice (R reference vs Rcpp core).}
\usage{
.smo_solve(
  K_acc,
  y,
  C,
  eps,
  tol = 0.001,
  max_iter = 100000L,
  n_check = NULL,
  n_freeze = 5L,
  alpha_init = NULL,
  alpha_star_init = NULL,
  warm_start_check = TRUE,
  new_mask = NULL,
  block_k4_enabled = TRUE,
  alpha_couple = 0.5,
  engine = c("rcpp", "r")
)
}
\description{
Forwards to the F7-C-full Rcpp core (\code{engine = "rcpp"}, default) or
the R reference implementation (\code{engine = "r"}). The R path is the
canonical algorithm and remains the bit-identical reference for the
Rcpp port; it will be deprecated in v0.0.4.0 and removed in v0.1.0
once the Rcpp path has cleared its graduation criteria (see CLAUDE.md
"engine = 'r' lifecycle").
}
\details{
Warm-start projection (Algorithm 1) runs in R via \code{.warm_start_init()}
BEFORE the core call, regardless of engine, so both paths see
already-feasible alpha/alpha* on entry.
}
\keyword{internal}
