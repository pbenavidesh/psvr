# Intentionally empty: rely on R's default optimization flags from
# ${R_HOME}/etc/Makeconf.win. CRAN policy prohibits package-set -O3 in
# Makevars (non-portable); Rtools45 already builds with -O2 by default.
